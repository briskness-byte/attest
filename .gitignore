node_modules

# Build files
dist/
*.zip
*.xpi

# Working files: built packages, the test browser profile, draft PR texts. None of it belongs in
# the source, and a stray `git add -A` once put a whole Firefox profile in a commit — cookies,
# places.sqlite, key4.db — because this rule had been broken by an append with no trailing newline.
var/
